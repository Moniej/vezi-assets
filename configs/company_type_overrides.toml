# Company-type overrides for the Valuation Engine (docs/fre/08_valuation_
# engine_architecture.md): "owner-judged, never AI-inferred" -- the same
# discipline already applied to configs/relation_taxonomy.toml's graph
# -provenance values and the (not-yet-built) news_outlets reliability
# registry.
#
# DELIBERATELY EMPTY as of this writing. Updated 2026-08-02, FSI Phase 23:
# securities.sector_ngx is now populated for 136/320 tickers (NGX's own
# official Daily Official List) -- but classify_company_type() deliberately
# does not consult it; mapping NGX's 13 sector headings onto this file's
# own company_type taxonomy is its own judgment call, not made here, and
# this file exists so the override mechanism is ready to receive
# owner-confirmed entries directly -- it is NOT this pass's place to guess
# which NGX tickers are banks/insurers/holdcos, however well-known some of
# them may seem (or how suggestive their own real sector_ngx value now is);
# an unconfirmed guess presented as a classification would be exactly the
# kind of unearned authority this platform's "owner-judged, never
# AI-inferred" rule exists to prevent. Every ticker defaults to "general"
# until an entry is added here by the owner.
#
# Format, once populated:
#   [overrides]
#   GTCO = "bank"
#   ...

[overrides]
